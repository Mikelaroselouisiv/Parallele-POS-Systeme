# Copier en .env.prod à côté de docker-compose.prod.yml / docker-compose.gcp.yml (ne pas committer .env.prod).

JWT_SECRET="remplace_par_un_secret_long_et_aleatoire"
POSTGRES_PASSWORD="remplace_par_un_mot_de_passe_fort"

# Même clé que la machine mère (sync-agent header X-Sync-Key)
SYNC_API_KEY="remplace_par_une_cle_sync_longue"
