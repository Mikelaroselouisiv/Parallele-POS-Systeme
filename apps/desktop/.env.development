# Tu n’as RIEN à modifier ici pour travailler en local.
# Par défaut, `npm run dev` appelle déjà http://localhost:3000 (voir `src/renderer/services/api.ts`).
#
# Uniquement si tu veux que le navigateur (mode dev) parle à Google Cloud au lieu du PC :
# 1) décommente la ligne ci-dessous, 2) redémarre `npm run dev`.
# VITE_API_URL=http://34.118.154.220
